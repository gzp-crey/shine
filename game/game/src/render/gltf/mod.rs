use crate::render::{IndexData, MeshData, ModelData, Vertex, VertexData, VertexP2C3};
use crate::utils::assets::{self, AssetError};
use crate::utils::url::Url;

pub fn load_from_url(url: &Url) -> Result<ModelData, AssetError> {
    unimplemented!()
}
